import styled from "styled-components";

export const Container = styled.div`
    width: max(60%, 300px);
    margin: 0 auto;
    position: absolute;
    top: 50px;
    left: 50%;
    transform: translateX(-50%);
`;